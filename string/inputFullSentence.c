#include <stdio.h>
#include<string.h>
//M33T

int main() {
    char str[100];
    

    printf("Enter a string: ");
    //scanf("%[^\n]s", str);
    gets(str);

    printf("%s",str);
    puts(str);

    return 0;
}

    

    